/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    basePath: '/resume',
    assetPrefix: '/resume/',
}

module.exports = nextConfig 